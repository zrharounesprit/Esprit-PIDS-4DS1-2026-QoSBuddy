# QoSBuddy — pin versions that work on Windows + Python 3.9
# Use a virtualenv to avoid broken global site-packages (pandas SystemError).
streamlit>=1.28
pandas==2.2.3
numpy==1.26.4
requests
fastapi>=0.100
uvicorn[standard]>=0.22
# Pydantic 2.12+ OpenAPI generation crashes on Python 3.9 (GenerateJsonSchema / typing _SpecialForm)
pydantic==2.9.2
pydantic-core==2.23.4
xgboost>=2.0
scikit-learn>=1.3
joblib
shap>=0.43
